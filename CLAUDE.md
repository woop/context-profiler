# CLAUDE.md

- Read `assignment.md` before proposing work.
- Use `docs/worklog.md` as an append-only coordination log. Multiple agents may append to this log. When writing to the log, assign yourself a persistent identity. Always read the log before writing to it.
- Use `docs/decisions.md` to capture durable long term decisions. If we change our minds, update the decision log.
- Front-load uncertainty in execution, then get to a thin end-to-end slice quickly.
- Keep the prototype self-contained and immediately usable.
- Do not browse or use hosted search tools unless explicitly approved.
